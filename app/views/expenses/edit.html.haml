%section
  .page-header
    .float-end
    %h1= t('titles.person', person: @person.full_name)
  = render 'shared/people_tabs'
  .row
    .col-md-12
      = render(partial: 'shared/expenses_form', locals: { person: @person, expense: @expense, url: person_expenses_path })
