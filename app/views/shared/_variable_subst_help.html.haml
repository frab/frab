.bg-light.rounded.p-3
  %h6.text-primary.mb-3
    %i.bi.bi-code-square.me-2
    = t('notifications_module.possible_variables')
  
  .variable-list
    - Notification::VARIABLES.each do |key, desc|
      .d-flex.justify-content-between.align-items-center.mb-2.p-2.bg-white.rounded.border
        .variable-info
          .fw-medium.text-dark= desc
        .variable-code
          %code.text-primary.user-select-all
            = "%{#{key}}"

  %style
    :css
      .user-select-all {
        user-select: all;
      }
