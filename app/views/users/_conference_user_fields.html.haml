.nested-fields
  = f.inputs do
    = f.input :role, as: :select, collection: ConferenceUser::ROLES
    = f.input :conference, as: :select, collection: accessible_conferences
    = remove_association_link :conference_user, f
    %hr/
