%section
  .page-header
    %h1= t('titles.create_account', {person: @person.full_name})
  = render 'shared/people_tabs'
  .row
    .span16
      = content do
        = inner do
          %p= t('people_module.no_user_account_for_person', {person: @person.full_name})

          = render 'form'
