- content_for :title do
  = link_to t('page_titles.change_password'), root_url, class: 'navbar-brand'

%section
  .page-header
    %h1= t('titles.forgot_your_password')

  .row
    .col-md-12
      = simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }, defaults:{error: false}) do |f|

        %div{class: "alert alert-danger alert-dismissible fade show", role: :alert}
          = render partial: 'devise/shared/error_messages', resource: resource

        %fieldset.inputs
          = f.input :email

        .actions
          = f.button :submit, class: 'btn btn-primary', value: t('devise.resend_password_instructions')

= render "devise/shared/links"
