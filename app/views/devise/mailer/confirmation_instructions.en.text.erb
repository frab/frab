Welcome <%= @resource.email %>!

You can confirm your account through the link below:

<%= cfp_user_confirmation_url(:locale => I18n.locale, :confirmation_token => @resource.confirmation_token, :conference_acronym => @resource.call_for_papers.conference.acronym) %>
