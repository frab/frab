<%= block do %>
  <h2><%= @conference.title %> - Call for Papers</h2>

  <%= content do %>
    <%= render "shared/flash", :flash => flash %>
    <% if @conference.call_for_papers.welcome_text %>
      <p><%= @conference.call_for_papers.welcome_text %></p>
    <% else %>
      <p>
        Welcome to the Call for Papers. To create or review your submissions you need to login. If you are new to <%= @conference.title %> you need to sign up for an account. 
      </p>
    <% end %>

    <h3>Log in with an existing account</h3>

    <p>You can sign in if you created an account for this Call for Papers, or for any previous event.</p>

    <%= semantic_form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>

      <%= f.inputs do %>
        <%= f.input :email %>
        <%= f.input :password %>
        <% if devise_mapping.rememberable? -%>
          <%= f.input :remember_me, :as => :boolean %>
        <% end -%>
      <% end %>
      
      <%= f.buttons do %>
        <%= f.commit_button "Sign in", :button_html => {:icon => "key", :class => :button} %>
      <% end %>

    <% end %>

    <h3>Sign up for a new account</h3>

    <p>If you are new to <%= @conference.title %>, please take the time to create an account. It is really easy and quick.</p>

    <p><%= action_button "user_edit", "Sign up", new_registration_path(resource_name) %></p>

    <div class="clear"></div>

    <h3>Forgot your password?</h3>

    <p>Been here before, but don't remember your password? We know how it is, so please feel free to</p>
    <%= action_button "email", "Request a new password", new_password_path(resource_name) %>

    <div class="clear"></div>
    
    <h3>I have registered but not received my confirmation instructions</h3>

    <p>This could be one of the rare occassions where an email has actually not been delivered. If you have already waited for some time and checked your spam folders, please
    </p>
    <%= action_button "email", "Request the confirmation instructions again", new_confirmation_path(resource_name) %>
    <div class="clear"></div>
    <p>We are sorry for any inconvenience.</p>

  <% end %>
<% end %>
