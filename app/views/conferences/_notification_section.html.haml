.bulk-notification
  - if e.count > 0
    %h3 #{ pluralize( e.count, "event" ) } in state #{ state }
    - noticket = e.select { |e| e.ticket == nil }
    - if noticket.count > 0
      =link_to 'Warning: ' + pluralize(noticket.count, "event" ) + ' without ticket', events_path(:event_state => state, 'q[s]' => 'ticket_id')
      %p
    - if noticket.count == e.count
      =button_to('no events', {}, { disabled: true, class: "btn disabled"})
    - else
      =button_to( 'Send ' + name + ' notifications to speakers in ' + pluralize( e.count - noticket.count, 'event'), { method: 'post' }, { class: "btn danger", data: { confirm: 'Are you sure you want to send ' + name + ' notifications for ' + pluralize(e.count - noticket.count, 'event') +'?', disable_with: 'working...' }})
  
