%section
  .page-header
    %h1= @first ? t(:create_your_first_conference) : t(:new_conference)
  - if @first
    .row
      .col-md-12
        .blank-slate
          %p= t('conferences_module.new_welcome')
  .row
    .col-md-12
      = render "form"
